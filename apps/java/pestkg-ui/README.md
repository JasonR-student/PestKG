# pestkg-ui

This module reserves the Vaadin UI boundary for a future split deployment. The
first runnable build keeps the initial Vaadin views in `pestkg-api` so API and
UI can be validated together against the tracked sample release.
