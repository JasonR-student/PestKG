# 本机运行记录：2026-09-02

## 已完成

- 从冻结发布版 `2026.08.3_federated` 生成 8 组论文图。
- 每组均包含 SVG、PDF 和 600 dpi PNG。
- 生成图 6 的节点、二部边、国家投影和实体投影 CSV。
- 生成中英文图注、完整 SHA-256 清单和 ZIP 交付包。
- 通过 5 项数据契约测试。
- 通过 Python 语法编译检查。
- 通过 `docker compose config` 静态配置验证。
- 中、日、韩文本地标签已使用对应 CJK 字体完成本机渲染。
- 图 5-8 的 PDF 已使用 Poppler 回渲为 PNG，并完成逐页视觉检查。

## Neo4j 运行状态

本机的 Docker Desktop Linux 引擎未启动。当前会话已请求启动 Docker Desktop，并等待 120 秒，但 `docker info` 仍无法连接 `dockerDesktopLinuxEngine`；因此本次图片实际使用发布 CSV 回退路径生成。

不得将本次结果描述为“已通过容器运行验证”。打开 Docker Desktop 并确认 `docker info` 成功后，执行以下命令完成 Neo4j 复核：

```powershell
docker compose -f research\figures\docker-compose.yml up --build --abort-on-container-exit renderer
```

容器流程会把发布样例装载为隔离的 `PaperSample` 子图，并把 200 条 Q1 结果装载为独立的 `PaperProjection` 派生层，再通过 Cypher 抽取 APVMA 子图和投影数据。该派生层仅用于可复现制图，不改变 PestKG 原有 ID 或核心谓词。
